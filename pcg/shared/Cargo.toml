[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.33.2", features = ["serde", "rand"] }
iced = {version = "0.14.0", features = ["canvas"]}
noise = "0.9.0"
rand = "0.10.2"
ron = "0.12.1"
serde = {version = "1.0.228", features = ["derive"]}
strum = "0.26"
strum_macros = "0.26"
uuid = {version = "1.23.1", features = ["v4", "serde"]}
